diff --git a/observability/docker-compose.yml b/observability/docker-compose.yml
index b7cb597..5b6211d 100644
--- a/observability/docker-compose.yml
+++ b/observability/docker-compose.yml
@@ -8,6 +8,8 @@ services:
     volumes:
       - ./prometheus.yml:/etc/prometheus/prometheus.yml
     restart: unless-stopped
+    networks:
+      - observability
 
   loki:
     image: grafana/loki:latest
@@ -16,7 +18,10 @@ services:
       - "3100:3100"
     volumes:
       - ./loki-config.yml:/etc/loki/local-config.yaml
+      - /loki:/loki
     restart: unless-stopped
+    networks:
+      - observability
 
   promtail:
     image: grafana/promtail:latest
@@ -26,6 +31,8 @@ services:
       - /var/lib/docker/containers:/var/lib/docker/containers:ro
       - /var/run/docker.sock:/var/run/docker.sock
     restart: unless-stopped
+    networks:
+      - observability
 
   grafana:
     image: grafana/grafana:latest
@@ -37,4 +44,10 @@ services:
     depends_on:
       - prometheus
       - loki
-    restart: unless-stopped
\ No newline at end of file
+    restart: unless-stopped
+    networks:
+      - observability
+
+networks:
+  observability:
+    driver: bridge
\ No newline at end of file
diff --git a/observability/loki-config.yml b/observability/loki-config.yml
index 3373893..d15d6a2 100644
--- a/observability/loki-config.yml
+++ b/observability/loki-config.yml
@@ -11,6 +11,8 @@ ingester:
       replication_factor: 1
   chunk_idle_period: 5m
   chunk_retain_period: 30s
+  wal:
+    dir: /loki/wal
 
 schema_config:
   configs:
